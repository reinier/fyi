<!doctype html>
<html>
    {% include '../head.njk' %}
    {% include '../header.njk' %}
    <body>
        <main>
            <article class="single type-link">
                <h1 class="link-title">
                    <span class="hostname">{{ href | urlHostname }} &bull; {{ page.date | readablePostDate }}</span>
                    <a href="{{ href }}" class="goto-link">{{ title }}</a>
                </h1>
                {{ content | safe }}
            </article>
        </main>
        <!-- Current page: {{ page.url | url }} -->
        {% include '../footer.njk' %}
    </body>
</html>
