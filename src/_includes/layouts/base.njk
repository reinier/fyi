<!doctype html>
<html lang="nl">
    {% include '../head.njk' %}
    {% include '../header.njk' %}
    <body>
        <main>
            <h1>{{ title }}</h1>
            {{ content | safe }}
        </main>
        <!-- Current page: {{ page.url | url }} -->
        {% include '../footer.njk' %}
    </body>
</html>
