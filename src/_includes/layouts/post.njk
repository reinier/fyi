<!doctype html>
<html>
    {% include '../head.njk' %}
    {% include '../header.njk' %}
    <body>
        <main>
            <article class="single">
                <h1>{{ title }}</h1>
                {{ content | safe }}
            </article>
        </main>
        <!-- Current page: {{ page.url | url }} -->
        {% include '../footer.njk' %}
    </body>
</html>
