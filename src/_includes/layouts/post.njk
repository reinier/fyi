<!doctype html>
<html>
    {% include '../head.njk' %}
    {% include '../header.njk' %}
    <body>
        <main>
            <article class="single type-article">
                <h1>{{ title }}</h1>
                <p class="post-date">
                    {{ page.date | readablePostDate }} &nbsp;&nbsp;&nbsp;&nbsp;✍️ Reinier Ladan
                </p>
                {{ content | safe }}
            </article>
        </main>
        <!-- Current page: {{ page.url | url }} -->
        {% include '../footer.njk' %}
    </body>
</html>
